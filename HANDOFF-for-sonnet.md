# HANDOFF REPORT — Saleem Rishah portfolio site (for the next Claude/Sonnet)

You are taking over a static portfolio site. Read this fully before acting. Communicate with the user (Saleem) in **Levantine Arabic**; keep technical terms in English. The user is NOT a programmer — explain simply, give exact click-by-click steps, never write Arabic inside code/comments (it broke JS twice via `/` chars — keep ALL code/comments English-only).

## Paths (Cowork mounts)
- Live site repo (linked to GitHub Desktop + Netlify): `D:\Work\saleemrishah`  → mount `/sessions/.../mnt/Work/saleemrishah/`
- Raw materials (NOT in git): `D:\Work\saleemrishah-materials`
- Plan doc (Arabic, user-facing): `D:\Work\saleemrishah\PLAN-خطة-المشروع.md`
- Content brief: `D:\Work\saleemrishah\CONTENT-BRIEF.md`

## Critical mount constraint
The Write/Edit tools TRUNCATE files on this mount (cut mid-file silently). ALWAYS write/patch files via `cat > file <<'EOF'` in bash, then `node -c` to verify syntax. Never trust Write for files >50 lines here.

## Stack
- Static HTML/CSS/JS. No framework. Hosted on Netlify, repo on GitHub (user: saleemrishah, repo: saleemrishah, branch main).
- CMS: Decap CMS at `/admin/` (Netlify Identity invite-only + Git Gateway, already enabled). Admin UI locale = en.
- Bilingual DE/EN via `.only-de`/`.only-en` spans + `[data-en]` attrs; `assets/main.js` toggles `html.de`/`html.en`. DE is default. NEVER write "fließend" for German (user is B2).
- Design system in `assets/style.css`: dark cinematic, vars --bg #0B0E12, --teal #46D5C2, --amber #E6A15C; fonts Bricolage Grotesque (display), Figtree (body), IBM Plex Mono (mono). Do not change design system without explicit request.

## How content works (IMPORTANT — recently changed)
- The site reads project content DIRECTLY from `content/` at runtime (see `assets/work.js` → `buildFromContent()`): it fetches `content/sections.json` (manifest: section meta + ordered list of item slugs per section) then each `content/<section>/<slug>.json`. Fallback chain: content/ → data/work.json → window.WORK_DATA (assets/work-data.js, minimal stub).
- Sections fixed order: ai, motion, brand, football. Section meta (titles/intros/eyebrows) lives in `content/sections.json` AND is regenerated by `build-work.js` (SECTION_META object).
- `build-work.js` runs on every Netlify deploy (netlify.toml: `command = "node build-work.js"`). It rebuilds BOTH `data/work.json` and `content/sections.json` by scanning content/ folders. So adding a project via CMS + deploy updates the manifest.
- Item JSON fields (flat, written by CMS): order, title_de, title_en, summary_de, summary_en, cover, chips[], body_de, body_en, video, gallery[], link_url, link_label, tracks[] (tracks only used by julia-kallas: 7 pipeline steps). `assets/work.js shapeItem()` maps these to the render shape.
- Other CMS-editable files: `content/site/branding.json` (logo_image, logo_text, logo_height, favicon — applied by assets/branding.js) and `content/site/home.json` (Hero portrait/headline/lead/status — applied by assets/home.js, index.html only).

## CURRENT OPEN ISSUES (user reports, verify each on the LIVE site after a fresh deploy)
The user insists everything is already pushed/deployed and NOT showing. I (previous Claude) could not confirm deploy state from here. FIRST STEP: have the user confirm in Netlify → Deploys that the LATEST commit actually built & published (green), and check the deploy log for whether `node build-work.js` ran. If the live `data/work.json`/`content/sections.json` are stale, the build isn't regenerating them — investigate netlify.toml build command + Node version on Netlify.

1. **AI section cards render empty/blank on live site** — section header shows but card area is blank; clicking the empty area opens Julia's modal. Likely: live site still serving old work.js/style.css (missing card CSS classes: button.work-card, .wc-media, .wc-title, .wc-play, .work-modal, .wm-gallery, .wm-video). Verify these classes exist in the DEPLOYED style.css. Local style.css HAS them (re-added this session). If live lacks them → deploy/cache issue.
2. **Cover image shows inside a 16:9 "video frame" with a play ▶ badge, but it's an image not a video** — in work.js, `.wc-play` badge should only render when `it.video` is truthy, and cover should render as plain `<img>` in `.wc-media`. Check the deployed work.js matches local. Item `content/ai/ai-generative-design.json` may have a stale `video` value or the old work.js is live.
3. **Gallery (secondary images) render small/ugly** — `.wm-gallery` uses `grid-template-columns:repeat(auto-fill,minmax(180px,1fr))` with first-image span trick. Tune sizing if needed after confirming correct CSS is live. Only julia-kallas has gallery so far.
4. **Logo broken** — `content/site/branding.json` had `logo_image: "assets/uploads/my-logo.pngheader.png"` (double extension typo from CMS upload). I set logo_image to "" so the text wordmark "Saleem Rishah" shows. User should re-upload a clean logo via CMS (Identity/Logo collection) and confirm the saved filename has a single extension.
5. **Credit burn** — user is at 20% of Netlify monthly credits (resets ~Monday). Cause: every CMS Publish triggers a deploy/build. RECOMMENDED FIX: enable Decap Editorial Workflow so the user can save drafts and publish once. Add to `admin/config.yml`: `publish_mode: editorial_workflow` (currently `publish_mode: simple`). Explain the new Draft/Review/Publish flow to the user. Also consider advising fewer deploys.

## Real content the user already wrote via CMS (keep it, it's good)
- `content/ai/julia-kallas.json`: title_de "Julia Kallas: KI-Persona von Null zur Mikro-Influencerin", strong DE/EN copy about a custom LoRA + local ComfyUI workflow + n8n automation, 12k followers in 3 months, cover julia image, gallery has images. This is exactly the tone wanted.

## User's strategic priority for ALL content
Emphasize **automation (n8n) + LOCAL AI (ComfyUI, LoRA training, local workflows)** — highly demanded in the German job market. Copy must prove he UNDERSTANDS the engineering (problem → technical decision & why → method → result), not just "uses tools". This applies to project descriptions AND the future CV.

## Verification habit (do this every change)
After any edit: `node -c` all touched JS; `python3 -c "import yaml;yaml.safe_load(open('admin/config.yml'))"`; render-test with jsdom (npm i jsdom --prefix /tmp/jt) simulating fetch from disk to confirm cards/modal/lang-toggle work and the user's latest CMS content shows. Confirm ZERO Arabic chars remain in any code file (js/yml/toml/html): `grep -rlP '[\x{0600}-\x{06FF}]' assets *.js *.toml admin` should only ever match content/*.json (DE text is fine there) — never code.

## Workflow with the user
- 3 separate chats planned: (1) technical/site [this], (2) content writing DE/EN, (3) CV redesign. See memory + CONTENT-BRIEF.md.
- The user pushes via GitHub Desktop (Commit to main → Push origin). He cannot run git/node himself. Give him the exact button steps.
- Batch all edits, push ONCE per session (one build) to save credits.
- Roadmap remaining: fix open issues above → fill content via CMS → content report → CV → security hardening (2FA, CAA, security headers) → connect domain saleemrishah.com (LAST step; not connected yet, old Adobe Portfolio still on the domain).

## First actions for you (next model)
1. Ask the user to confirm Netlify latest deploy is green AND open the deploy log to see if build-work.js ran and what files it wrote.
2. If deploy is fine but content still stale → the live data/work.json/sections.json aren't regenerating; debug build command / Node version (add `[build.environment] NODE_VERSION="20"` to netlify.toml if missing).
3. Apply Editorial Workflow (publish_mode) to stop credit burn.
4. Re-verify issues 1-3 on the live URL https://saleemrishah.netlify.app/work after a clean deploy.
